export enum Measureunits {
  KILOGRAM = 'Kg',
  GRAM = 'g',
  METER = 'm',
  CENTIMETER = 'cm',
  MILLIMETER = 'mm',
  LITER = 'L',
  INCH = 'in'
}

export enum DataType {
  TEXT = 'text',
  NUMBER = 'number',
  BOOLEAN = 'bool',
}
